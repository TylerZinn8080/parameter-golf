# 2026-04-XX SP8192 PreQuantTTT NgramTilt

This folder contains a standalone `train_gpt.py` intended to be run via:

```bash
torchrun --standalone --nproc_per_node=8 train_gpt.py
```

Fill in results after training.

